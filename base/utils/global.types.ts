export enum ScrollDirections {
  left = 'scrollLeft',
  bottom = 'scrollBottom',
}
export enum Variants {
  primary = "primary",
  success = "success",
  danger = "danger",
  warning = "warning",
  white = "white",
  secondary = "secondary"
}
